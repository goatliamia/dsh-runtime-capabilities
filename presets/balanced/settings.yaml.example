# Balanced — 事后 guard + circuit + reconcile：明显无进展时停止；可疑结果做必要检查。
# 事前开关按需打开：continuation: true 表示「唯一确定的下一步交给 Runtime」。
# 职责组合（docs/17 §5、docs/18 §3）：
#   guard      已知非法动作拦截
#   circuit    连续无进展熔断（消费 Progress 的 stalled）
#   reconcile  副作用可能已发生时不盲目重试（failure + progressed）
agent-presets:
  default: balanced
runtime-seam:
  preset: balanced
  continuation: false
  authority: false
  circuitThreshold: 2
