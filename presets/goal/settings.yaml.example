# Goal — goal 能力开关（goal 段独立存在，不作为一级事后模式）。
# Runtime Goal 窄版 v1：Current + Desired + known transition；只做 announce 与 guard。
agent-presets:
  default: goal
runtime-seam:
  preset: goal
  continuation: false
  authority: true
  circuitThreshold: 2
