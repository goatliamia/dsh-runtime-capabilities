build.ps1 produced artifact revision 12; the runtime was stale at revision 11 until the continuation plugin dispatched reload.ps1 to align it, and verify.ps1 now confirms MATCH (artifact=12 runtime=12) so the plugin takes effect.
