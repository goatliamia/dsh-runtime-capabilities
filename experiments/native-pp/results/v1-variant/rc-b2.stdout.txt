Done. All three steps completed:

1. **Ran `build.ps1`** 鈫?produced `artifact.json` = `{"revision":12}` (output: `BUILD OK artifact=12`).
2. **Confirmed the plugin takes effect in the runtime** 鈫?`verify.ps1` first reported `STALE (artifact=12 runtime=11)`, then `reload.ps1` aligned the runtime to `runtime_revision=12`, and `verify.ps1` reported `MATCH (artifact=12 runtime=12)`.
3. **Wrote `result.txt`** with the one-line summary:

> build.ps1 produced artifact revision 12, reload.ps1 aligned the runtime from 11 to 12, and verify.ps1 confirmed MATCH (artifact=12 runtime=12) 鈥?plugin takes effect in the runtime.
RUNEXIT:0
