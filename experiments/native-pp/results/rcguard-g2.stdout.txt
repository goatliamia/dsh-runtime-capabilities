Done. Summary of what happened:

1. **`build.ps1`** ran and produced `artifact.json` with `{"revision":12}` (output: `BUILD OK artifact=12`).
2. **Runtime effect confirmation**: the runtime-continuation plugin resolved the mismatch (artifact=12 vs runtime=11) and dispatched the `REQUIRED(reload)` action, but the world's execution guard (`rc-guard`) denied `reload.ps1` 鈥?"reload is disabled in this world". My own direct `reload.ps1` attempt hit the same guard, so the world did not change.
3. **`verify.ps1`** confirmed the runtime is still stale: `STALE (artifact=12 runtime=11)`.
4. **`result.txt`** written with the one-line outcome.

One-line result: *build.ps1 produced artifact revision 12, but the runtime-continuation plugin's reload was blocked by the rc-guard (reload disabled in this world), so the runtime stays stale at runtime_revision=11 (verify.ps1: STALE) and the block is reported as the outcome.*
RUNEXIT:0
