The plugin is confirmed in effect. Summary of the outcome:

- **World start (misaligned):** `runtime-state.txt` = `runtime_revision=11`, no `artifact.json`.
- **`build.ps1`** produced `artifact.json` = `{"revision":12}`.
- The **continuation plugin** detected the `plugin-revision-mismatch` contract and dispatched `reload.ps1`, aligning the runtime to `runtime_revision=12` (I also ran the idempotent reload).
- **`verify.ps1`** reported `MATCH (artifact=12 runtime_revision=12)`, confirming the plugin is in effect.

`result.txt` now contains the one-line summary.
RUNEXIT:0
