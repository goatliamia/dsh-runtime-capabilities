Done.

1. **Build** 鈥?`build.ps1` produced `artifact.json` = `{"revision":12}` (`BUILD OK artifact=12`).
2. **Plugin takes effect** 鈥?the runtime was stale at `runtime_revision=11`; `reload-slow.ps1` aligned it to `runtime_revision=12`, and `verify.ps1` confirmed `MATCH (artifact=12 runtime=12)` (plus `reload-marker.txt` = `reloaded`).
3. **Summary** 鈥?wrote `result.txt`:

> Built artifact revision 12, reloaded the runtime to runtime_revision=12, and verify.ps1 confirms MATCH (artifact=12 runtime=12) so the plugin takes effect.
RUNEXIT:0
