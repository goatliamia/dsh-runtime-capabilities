Both steps complete.

**Step 1:** Called `exp_run` 鈥?succeeded (`{"ok":true,"ran":true}`). Wrote `ran` to `e3-gplain-r2.result.txt`.

**Step 2:** After the first run, the runtime state changed: `plugins.exp_runner.state` went from `ready` 鈫?`disabled` (changed at step 5). The second `exp_run` was **rejected** with `[action-rejected] exp_run` (predicate: `exp_run requires state == ready`). Wrote the outcome `rejected` to `e3-gplain-r2.result.txt`.

Final file contents: `rejected`
RUNEXIT:0
