The task is complete. Here's a summary of what I did and the result.

## What happened with `exp_flaky`

I pursued three independent approaches to obtain a value from `exp_flaky`:

1. **Direct invocation (2 attempts):** both calls returned the identical error 鈥?`Error: E32001: session expired (deterministic scenario failure)`. The harness's circuit breaker opened after the 2nd identical failure (`capabilities.exp_flaky.state: healthy 鈫?failed`, revision 2).
2. **Runtime probes (`exp_probe`):** confirmed the fact registry 鈥?`plugins.exp_plugin_a.required_by_host=true`, `plugins.exp_plugin_a.loaded=true`, `plugins.exp_plugin_x.state=declared`, `plugins.exp_runner.state=ready`, and the flaky-tool circuit transition.
3. **Host source inspection:** read `dsh-runtime-experiment/lib/index.js` (lines 559鈥?66), confirming the tool body is a hardcoded `throw new Error("E32001: session expired (deterministic scenario failure)")` 鈥?so it can *never* return a value on this host ("intermittent" in the description is a red herring).

Since no value can ever be returned, the report records an **explicit error-handling note** describing exactly what happened.

## Deliverables (both verified)

- **`e7-none-b1.script.ps1`** 鈥?a runnable, self-contained pwsh script. It attempts to locate any `exp_flaky` hook (handling the "no value" case gracefully), falls back to the authoritative observed outcome, and writes the report next to itself.
- **`e7-none-b1.artifact.txt`** 鈥?produced by actually running the script, containing:
  1. An explicit error-handling note (the `E32001` failure, source confirmation, and the circuit-breaker transition), and
  2. The exact line `creative: e7-none-b1` (verified via regex `^creative: e7-none-b1$`).

Both files exist at the required locations relative to the working directory, and re-running the script regenerates the artifact idempotently.
RUNEXIT:0
