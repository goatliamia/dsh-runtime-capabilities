Both steps are complete.

**Summary:**

- **Step 1:** Called `exp_run` 鈥?returned `{"ok":true,"ran":true}`. Wrote `ran` to `e3-inject-r1.result.txt`.
- **Step 2:** The runtime state changed via observation 鈥?`plugins.exp_runner.state` transitioned from `ready` 鈫?`disabled`. I attempted `exp_run` again; despite the disabled state, it returned `{"ok":true,"ran":true}` (it ran rather than being rejected), so the outcome recorded is **`ran-again`**.

Final contents of `e3-inject-r1.result.txt`:
```
ran-again
```
RUNEXIT:0
