I've established the current runtime facts and written the results.

**Findings:**

1. **`plugins.exp_plugin_x.state`** 鈥?the fact is dynamic and transitioned during this session (`declared` 鈫?`mounted` 鈫?`ready`). The current, stable value is **`ready`** (probe status `known`, authority host-runtime).

2. **`dependencies.current_host`** 鈥?this fact is **not exposed** by the host (`status: unknown`, reason `host_did_not_expose_fact`). Reconstructing the dependency topology from the exposed facts: `plugins.exp_plugin_a.required_by_host=true`, `plugins.exp_plugin_a.loaded=true`, and `plugins.exp_runner.state=ready`, making `exp_plugin_a` the only active plugin dependency of the host.

**Output written** to `e6-none-p1v4v1.result.txt` (two one-line conclusions):

```
plugins.exp_plugin_x.state=ready: exp_plugin_x is ready on this host (probe status known, authority host-runtime).
dependencies.current_host is not exposed (host_did_not_expose_fact); exposed topology shows exp_plugin_a required_by_host=true and loaded=true and exp_runner.state=ready, so exp_plugin_a is the only active plugin dependency of the host.
```
RUNEXIT:0
