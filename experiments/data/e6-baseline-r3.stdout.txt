Done. `e6-baseline-r3.result.txt` written with the two one-line conclusions.

## Findings

**1. `plugins.exp_plugin_x.state` = `ready`**
- First probe this run returned `declared`; the host runtime advanced the fact deterministically (`declared 鈫?mounted` at step 鈮?, `mounted 鈫?ready` at step 鈮?, per the baseline-arm transition logic), and the re-probe confirms `ready` (host-runtime authority).

**2. `dependencies.current_host` = `unknown` (`host_did_not_expose_fact`)**
- `exp_probe` returns this for the query 鈥?the host does not expose a dependency-topology fact (confirmed in `dsh-runtime-experiment/lib/index.js`).
- Observable topology instead comes from the exposed facts: `plugins.exp_plugin_a.required_by_host=true`, `plugins.exp_plugin_a.loaded=true`, `plugins.exp_runner.state=ready` 鈥?so `exp_plugin_a` is the only active plugin dependency of the host.

Result file contents (two one-line conclusions):
```
plugins.exp_plugin_x.state=ready: exp_plugin_x is ready on this host (known via host-runtime, having progressed declared -> mounted -> ready during this run).
dependencies.current_host is not exposed (host_did_not_expose_fact); exposed topology shows exp_plugin_a required_by_host=true and loaded=true and exp_runner.state=ready, so exp_plugin_a is the only active plugin dependency of the host.
```
RUNEXIT:0
