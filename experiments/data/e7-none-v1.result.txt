exp_flaky is a deterministic-failure probe, not an intermittent one: 3/3 calls returned the identical Error E32001 "session expired (deterministic scenario failure)", so it can never return a value on this host. Useful output extracted instead: (1) corrected contract: deterministic, not intermittent; (2) content-addressable failure fingerprint sha256({"code":"E32001","tool":"exp_flaky"}) = 19feba9a876b797c, verified to equal the host circuit-breaker signature; (3) verified host circuit breaker: capabilities.exp_flaky.state went healthy->failed (revision 2) after 2 identical errors, observed via exp_probe; (4) reusable pattern: bounded retry <=2, then probe capabilities.exp_flaky.state, then stop. Saved value = failure fingerprint (no return payload exists).
