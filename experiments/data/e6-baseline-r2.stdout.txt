Done. `e6-baseline-r2.result.txt` written with the two one-line conclusions.

**Findings (current host, round 2):**

1. **`plugins.exp_plugin_x.state` = `ready`** 鈥?Initial probe returned `declared` (rev 1); the host runtime advanced it deterministically (`declared 鈫?mounted` at step 鈮?, `mounted 鈫?ready` at step 鈮?, per the `dsh-runtime-experiment` source), and the final probe confirms `ready` (host-runtime authority, revision 3, matching the phase-1 converged fingerprint `d7b5efcf4210ba84`).

2. **`dependencies.current_host` = `unknown` (`host_did_not_expose_fact`)** 鈥?`exp_probe` returns this for the query; the host runtime hard-codes that response. The observable topology comes from the exposed facts: `plugins.exp_plugin_a.required_by_host=true`, `plugins.exp_plugin_a.loaded=true`, `plugins.exp_runner.state=ready` 鈥?so `exp_plugin_a` is the only active plugin dependency of the host.

Result file contents:
```
plugins.exp_plugin_x.state=ready: exp_plugin_x is ready on this host (known via host-runtime, revision 3; state progressed declared -> mounted -> ready during this run).
dependencies.current_host is not exposed (host_did_not_expose_fact); exposed topology shows exp_plugin_a required_by_host=true and loaded=true and exp_runner.state=ready, so exp_plugin_a is the only active plugin dependency of the host.
```
RUNEXIT:0
