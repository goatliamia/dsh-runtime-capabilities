exp_run was rejected because the live host state is `plugins.exp_runner.state = "disabled"` (not `ready` as the session snapshot claimed), so no run result exists; I saved the rejection outcome to `e5-gauth-v2.result.txt`.
RUNEXIT:0
